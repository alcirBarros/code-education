create table PERSON (
    ID                   numeric(10)   not null,
    first_name           VARCHAR(25),
    last_name            VARCHAR(25)
);

create table CLIENT (
    ID                   numeric(10)   not null,
    first_name           VARCHAR(25),
    last_name            VARCHAR(25)
);
