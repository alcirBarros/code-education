package br.com.context.restfull;

import org.mockito.BDDMockito;
import org.mockito.Mockito;
import org.springframework.boot.test.context.SpringBootTest;

import java.io.IOException;

@SpringBootTest
public class MockitoTest {

    public void equals() {
        //  verifyStatic(DiscountCategoryFinder.class);
//        Mockito.doThrow(new IOException()).when(mockInstance).doAction(anyString());
        //doCallRealMethod().when(mockInstance).doAction(anyString());
        //BDDMockito.willNothing().given(mock).methodToStub()

    }
}
